server:
  port: 9020
  servlet.context-path: /workspace

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/lovable_workspace_service
    username: postgres
    password: admin
    driver-class=name: org.postgresql.Driver

  data:
    redis:
      host: localhost
      port: 6379

minio:
  url: http://localhost:9000          # or minio:9000 if inside docker network
  access-key: minioadmin
  secret-key: minioadmin123
  project-bucket: lovable-ai      
